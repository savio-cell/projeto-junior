// 1 obejetivo = mult a  quantia de quantos lanches vao pedir 
// 1- passo pegar a tag (p) e fazer um loop paara sempre somar mais um ate 10
    while(let = i = 0);
    if (i < 10) {
        console.log(i)++;
    }